export default class Sphere{
    constructor(name, center, radius, color, specular, reflective){
        this.name = name;
        this.center = center;
        this.radius = radius;
        this.color = color;
        this.specular = specular;
        this.reflective = reflective;
    }
}